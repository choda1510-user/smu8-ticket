services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.test
    container_name: frontend
    ports:
      - "80:80"
    networks:
      - smu8-ticket-network
  backend-ticket:
    build:
      context: ./backend-ticket
      dockerfile: Dockerfile.test-waiting
    container_name: backend-ticket
    ports:
      - "8080:8080"
    depends_on:
      database:
        condition: service_healthy
    environment:
      - SPRING_PROFILES_ACTIVE=test-waiting
      - TZ=UTC
    networks:
      - smu8-ticket-network
    volumes:
      - backend_file_dir:/app/upload-dir
  backend-waiting:
    build:
      context: ./backend-waiting
      dockerfile: Dockerfile.test-waiting
    container_name: backend-waiting
    ports:
      - "8081:8081"
    environment:
      - SPRING_PROFILES_ACTIVE=test-waiting
    networks:
      - smu8-ticket-network
  database:
    build:
      context: ./database
      dockerfile: Dockerfile.test
    container_name: database
    ports:
      - "3306:3306"
    networks:
      - smu8-ticket-network
    volumes:
      - mysql_test_database:/var/lib/mysql
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost', '-u', 'root', '-p$$root']
      timeout: 20s
      retries: 10
  redis-ticket:
    build:
      context: ./redis-ticket
      dockerfile: Dockerfile.dev
    container_name: redis-ticket
    ports:
      - "6379:6379"
    networks:
      - smu8-ticket-network
  redis-waiting:
    build:
      context: ./redis-waiting
      dockerfile: Dockerfile.test-waiting
    container_name: redis-waiting
    ports:
      - "6381:6379"
    networks:
      - smu8-ticket-network
    volumes:
      - redis_waiting_volume:/data
volumes:
  mysql_test_database:
  redis_waiting_volume:
  backend_file_dir:
networks:
  smu8-ticket-network:
    driver: bridge